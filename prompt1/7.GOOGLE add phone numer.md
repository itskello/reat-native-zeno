Read AGENTS.md first and follow it strictly.

We identified a gap in the Google sign-up flow: when a user signs up via 
"Login With Google", Clerk creates the account with email only — no phone 
number is collected. The flow currently redirects Google sign-up users 
straight to "Verify Phone Number", but Clerk has no phone number to send 
a code to, so this breaks.

Implement a new screen, "Add Your Phone Number", inserted into the flow 
only for Google sign-ups (direct email/password sign-ups already collect 
the phone number on the Create Account screen and skip this new screen).

Layout: back button (top-left), headline "Add Your Phone Number", subtext 
"We need your phone number to verify your account and keep it secure", a 
"Phone number" field with country flag + dial code prefix (pre-filled based 
on the country selected earlier on "Where Do You Live", consistent with the 
existing Create Account screen's phone field behavior), and a "Continue" 
primary button.

Behavior:
- This screen only appears in the Google sign-up path, inserted right after 
  Google OAuth completes and before "Verify Phone Number".
- Validate the phone number against the selected country's format/length.
- "Continue" is disabled until a valid phone number is entered.
- On "Continue", attach the phone number to the user's Clerk account (Clerk 
  phone number creation API), then proceed to "Verify Phone Number" as normal 
  — the code is now sent to this newly added number.
- Back button returns to the Welcome screen (since there's no prior 
  form step in the Google sign-up path to go back to).

Updated flow logic:
- Google sign-up: Welcome → Login With Google (OAuth) → Add Your Phone 
  Number (new) → Verify Phone Number → Set Up PIN → Biometric/Fingerprint 
  Setup → Onboarding Complete.
- Direct email sign-up: Welcome → Where Do You Live → Create Account (phone 
  collected here) → Verify Phone Number → Verify Email → Set Up PIN → 
  Biometric/Fingerprint Setup → Onboarding Complete.

⚠️ Important open question — please confirm before implementing: since 
Google sign-up skips "Where Do You Live" entirely, this screen currently has 
no prior country selection to pre-fill the dial code from. Either:
(a) this screen must also let the user pick their country/dial code manually 
    (no pre-fill), or
(b) "Where Do You Live" needs to be inserted into the Google sign-up path too, 
    before this screen, so the country is known.
Flag this and wait for confirmation on which approach to use.

No exact design reference exists yet for this screen — build it using 
ZENO's existing design system (see AGENTS.md) and match the visual style of 
the Create Account screen's phone number field exactly.

@prompt_material/7.GOOGLE add phone numer.png